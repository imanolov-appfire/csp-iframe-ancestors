app:
  runtime:
    name: nodejs22.x

modules:
  jira:projectPage:
    - key: csp-iframe-project-page
      resource: main-resource
      title: CSP Iframe Repro
      layout: blank

resources:
  - key: main-resource
    path: static/hello-world/build

permissions:
  content:
    styles:
      - 'unsafe-inline'
    scripts:
      - 'unsafe-inline'

